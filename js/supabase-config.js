/* Fill these in with your Supabase project's values:
   Supabase dashboard -> Project Settings -> API -> "Project URL" and "anon public" key.
   The anon key is meant to be public/client-side; access is controlled by the
   Row Level Security policies in supabase/schema.sql, not by hiding this key. */
window.SUPABASE_CONFIG = {
  url: "https://vhusyuefqnmczmtksypr.supabase.co",
  anonKey: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZodXN5dWVmcW5tY3ptdGtzeXByIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI4NzI3NjksImV4cCI6MjA5ODQ0ODc2OX0.AcDovu_k3-bHgsUQg48RsroJc_yAghns2PlVCEv46RE"
};

/* One shared code everyone on staff types in to log in (along with their own
   name, so the History tab still shows who did what). Change this to
   whatever you like -- it's checked in the browser, not in the database. */
window.TEAM_ACCESS_CODE = "FuegosLA";
